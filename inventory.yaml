webservers:
  hosts:
    fish:
      ansible_user: "{{ ansible_user }}"
      ansible_port: "{{ ansible_port }}"
      ansible_become_pass: "{{ ansible_become_pass }}"
    cat:
      ansible_user: "{{ ansible_user }}"
      ansible_port: "{{ ansible_port }}"
      ansible_become_pass: "{{ ansible_become_pass }}"